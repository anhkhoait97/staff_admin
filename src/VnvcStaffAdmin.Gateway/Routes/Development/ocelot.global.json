{
  "GlobalConfiguration": {
    "BaseUrl": "http://localhost:50004",
    "RateLimitOptions": {
      "ClientIdHeader": "X-ClientId",
      "EnableRateLimiting": true,
      "GeneralRules": [
        {
          "Endpoint": "*",
          "Period": "1s",
          "Limit": 5
        }
      ]
    }
  }
}